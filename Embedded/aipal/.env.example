TELEGRAM_BOT_TOKEN=

# Comma-separated list of Telegram User IDs allowed to use the bot.
# If empty or not set, the bot will be open to everyone.
ALLOWED_USERS=12345678,87654321

# Optional: force agent working directory (project root).
# Can also be changed at runtime with /project.
# AIPAL_AGENT_CWD=/absolute/path/to/project

# Optional: speech-to-text command used for Telegram audio/voice notes.
# Default: parakeet-mlx
# AIPAL_WHISPER_CMD=parakeet-mlx

# Optional: when true (default), drops queued Telegram updates on startup.
# Set to false if you want to process pending updates while bot was offline.
AIPAL_DROP_PENDING_UPDATES=true
